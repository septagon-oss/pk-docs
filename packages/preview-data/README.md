# Preview Data

This package is reserved for future fixture snapshots, synthetic showcases, and cross-repo preview data that should not live inside the renderer itself.

The current first slice uses `.generated/` directly so the docs app exercises real module bundles.
